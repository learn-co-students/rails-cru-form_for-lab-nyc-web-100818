<h1>Show Song</h1>

<h1><%= @song.name %></h1>
Artist: <%= link_to @song.artist.name, artist_path(@song.artist.id) %><br>
Genre: <%= link_to @song.genre.name, genre_path(@song.genre.id) %></br>

<%= link_to 'Edit Song', edit_song_path(@song) %>
