<h1><%= @genre.name %></h1>
<%= link_to 'Edit', edit_genre_path(@genre) %>
<h2>Songs</h2>
<ul>
  <% @genre.songs.each do |s| %>
    <li>
      <%= link_to s.name, song_path(s) %>
    </li>
  <% end %>
</ul>